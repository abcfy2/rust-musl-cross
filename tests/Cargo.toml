[package]
authors = ["root"]
name = "test-musl"
version = "0.1.1"

[dependencies]
env_logger = "0.7.1"
error-chain = "0.12.4"
openssl-probe = "0.1.2"
reqwest = { version = "0.10", features = ["blocking"] }
